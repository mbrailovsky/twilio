<%= form_for(security_question) do |f| %>
  <% if security_question.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(security_question.errors.count, "error") %> prohibited this security_question from being saved:</h2>

      <ul>
      <% security_question.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :question %>
    <%= f.text_field :question %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
